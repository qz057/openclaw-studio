# Phase 4 Runtime Closeout - 2026-04-26

## Scope

Phase 4 deepened the release candidate from styled RC into runtime-observed RC:

- Codex page uses local `~/.codex` config/session probes through Electron shell state.
- Agents page uses local OpenClaw config, session records, cron observations, and Codex handoff hints when available.
- Dashboard replaced the generic success/latency collector gaps with real runtime sidecar sampling: fulfilled call ratio, metric batch duration, gateway/Hermes batch duration, and rolling resource samples.
- OpenClaw/Hermes gateway errors now include the likely cause and the next command-level action.
- RC package QA now includes direct zip extraction, first launch, shutdown, and restart.

## Changed Files

- `apps/studio/src/hooks/useDashboardRealtimeData.ts`
- `apps/studio/electron/runtime/probes/gateway-services.ts`
- `apps/studio/electron/runtime/wsl-hermes-reader.ts`
- `apps/studio/scripts/phase4-rc-smoke.cjs`
- `apps/studio/package.json`
- `.tmp/visual-check.mjs`
- `delivery/openclaw-studio-rc-manifest-20260426.md`
- `delivery/phase4-rc-smoke-20260426.json`

## Runtime Probe Result

The built runtime was probed directly after Phase 4 changes:

- Electron shell smoke: `bridge=hybrid`, `runtime=ready`, `sessions=3`, `codexTasks=6`.
- OpenClaw models: selected `relay/gpt-5.4`, 4 options.
- Hermes models: selected `relay/gpt-5.4`, 4 options.
- OpenClaw gateway status command currently times out in WSL; UI now reports the timeout and tells the user to run `openclaw gateway status` inside WSL.
- Hermes gateway status command currently times out in WSL; UI now reports the timeout and tells the user to run `hermes gateway status` inside WSL.
- OpenClaw chat state can read the latest session history, but send is blocked until the WSL send path responds.
- Hermes state is blocked until the gateway/runtime entry is available; session reader failures are now quiet fallback instead of noisy console output.

## Verification

```powershell
npm run -C "E:\claucd\界面控制台程序\apps\studio" typecheck
npm run -C "E:\claucd\界面控制台程序\apps\studio" test
npm run -C "E:\claucd\界面控制台程序\apps\studio" build
npm run -C "E:\claucd\界面控制台程序\apps\studio" smoke
node "E:\claucd\界面控制台程序\.tmp\visual-check.mjs"
npm run -C "E:\claucd\界面控制台程序\apps\studio" dist:win
npm run -C "E:\claucd\界面控制台程序\apps\studio" package:smoke
npm run -C "E:\claucd\界面控制台程序\apps\studio" phase4:rc-smoke
```

Results:

- Typecheck: passed.
- Tests: 8 files passed, 67 tests passed.
- Build: passed.
- Smoke: passed.
- Visual matrix: 36 checks passed, 0 failures; screenshots under `.tmp/ui-shots`.
- `dist:win`: passed.
- `package:smoke`: passed; packaged app stayed alive for 20000ms.
- `phase4:rc-smoke`: passed; zip extraction, first launch, shutdown, and restart verified.

## Artifacts

| Artifact | Path | Size | SHA256 |
|---|---|---:|---|
| Windows zip | `apps/studio/release/OpenClaw Studio-0.1.0-win-x64.zip` | 141157276 bytes | `1B22175E8A712938F2AFCE2369EAB445B4AE6BCF5244B2D6A2EF0589468CA56E` |
| Unpacked exe | `apps/studio/release/win-unpacked/OpenClaw Studio.exe` | 222973952 bytes | `50730C076C408878624E0284FCC932048B6EBCD76F9EFB0F9C34E6851321FF8B` |
| RC smoke report | `delivery/phase4-rc-smoke-20260426.json` | - | included in repo output |
| Screenshots | `.tmp/ui-shots/*-cdp.png` | 36 files | generated by CDP visual matrix |

## Release Decision

Phase 4 RC is acceptable as an unsigned Windows zip/portable candidate.

Phase 5 has since closed the WSL gateway timeout risk for the current machine. Installer/signing can move forward after confirming target-user WSL prerequisites.
